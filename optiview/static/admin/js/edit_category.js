document.addEventListener("DOMContentLoaded", () => {
  const form = document.getElementById("editCategoryForm");

  form.addEventListener("submit", () => {
    // Optional: add loading state later
    console.log("Category update submitted");
  });
});
